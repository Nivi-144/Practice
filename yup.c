#include <stdio.h>
int x[10], a[10] [10],n;
void nextValue{
  while(1){
    x[k]=(x[k-1]%(n+1));
    if(x[k]==0){
      return;
    }
    if(a[x[k-1][x[k]]!=0)
      {
        int j;
        for(j=1;j<=k;j++){
          if(x[j]==x[k]){
            break;
          }
          if(j==k)
          {
              if(k<n||k==n&&a[x[n]][x[1]]!=0)
              return;
          }
        }
      }
  }
}
